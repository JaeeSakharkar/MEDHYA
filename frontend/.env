# AWS Cognito Hosted UI domain (no quotes)
VITE_COGNITO_DOMAIN=medhya.auth.us-east-1.amazoncognito.com

# AWS Cognito App Client ID (no quotes)
VITE_COGNITO_CLIENT_ID=6npa9g9it0o66diikabm29j9je

# Redirect after login (must match Cognito allowed callback URLs)
VITE_REDIRECT_URI=http://localhost:8081/callback

# Redirect after logout (must match Cognito allowed sign-out URLs)
VITE_LOGOUT_URI=http://localhost:8081/login

# Your backend API base URL (for REST calls)
VITE_API_BASE_URL=http://localhost:5000
