# JWT Secret (For local dev. Ignore if only using Cognito!)
JWT_SECRET=yourDevSecret123

# AWS Cognito (Authentication) - You need to add your actual User Pool ID
# Find it in AWS Cognito Console -> User pools -> Your pool -> General settings
COGNITO_POOL_ID=us-east-1_RsOYVRSJu
COGNITO_CLIENT_ID=6npa9g9it0o66diikabm29j9je
COGNITO_REGION=us-east-1
COGNITO_DOMAIN=medhya.auth.us-east-1.amazoncognito.com

# DynamoDB Table (Legacy - keeping for fallback)
DYNAMODB_TABLE=MedhyaTable

# MongoDB Connection (Atlas Cloud Database)
MONGODB_URI=mongodb+srv://jaeesakharkar02:9321677013@cluster0.6arixht.mongodb.net/quizmaster?retryWrites=true&w=majority&appName=Cluster0

# Database Type (mongodb or dynamodb)
DB_TYPE=mongodb

# Development mode (allows public access to APIs)
NODE_ENV=development
DEV_MODE=true

# SNS Topic (Notifications)
SNS_TOPIC_ARN=arn:aws:sns:us-east-1:123456789012:QuizReminders

# AWS Region (used by all AWS SDKs)
AWS_REGION=us-east-1

# API Gateway URL (if deployed via Lambda/APIGW)
API_URL=https://xxxxxxxx.execute-api.us-east-1.amazonaws.com/prod

# Port config for local server
PORT=5000

# (Optional) For debugging AWS SDK
DEBUG=false

# (Add other configs as you need)
